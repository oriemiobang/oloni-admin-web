import type { Metadata } from 'next';
import { Inter } from 'next/font/google';
import { Providers } from './providers';
import './globals.css';

const inter = Inter({
  subsets: ['latin'],
  variable: '--font-sans',
  display: 'swap',
});

export const metadata: Metadata = {
  title: {
    default: 'Oloni Admin',
    template: '%s | Oloni Admin',
  },
  description:
    'Control room for the Oloni Gambella Mobility & Delivery Platform — manage drivers, bookings, airport shuttle, finance, and operations.',
  robots: { index: false, follow: false }, // Admin — never publicly indexed
};

export default function RootLayout({ children }: Readonly<{ children: React.ReactNode }>) {
  return (
    <html lang="en" className={`${inter.variable} h-full antialiased`} suppressHydrationWarning>
      <body className="bg-background text-foreground flex min-h-full flex-col">
        <Providers>{children}</Providers>
      </body>
    </html>
  );
}
